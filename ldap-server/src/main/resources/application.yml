spring:
  application:
    name: ldap-server
  banner:
    location: http://localhost:8888/config/banner.txt
  config:
    import:
      - configserver:http://localhost:8888/config
  threads:
    virtual:
      enabled: true